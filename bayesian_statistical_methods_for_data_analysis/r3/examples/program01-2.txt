  #===== Program 1.2（正規乱数の発生とファイルへの保存）=============
  setwd("C:/RW")
  # 乱数の個数と分布のパラメータを設定
  N <- 1000; mean <- 1; sd <- 0.5
  # 1000個の N(1, 0.25) の乱数を発生し，ベクトル x に与える
  x <- rnorm(N, mean, sd)
  # 発生した乱数を random1.txt というテキストファイルに書き込む
  write(x, "random1.txt", ncolumns=1)
  #==============================================================