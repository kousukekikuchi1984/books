  #===== Program 1.3（ファイルのデータをRへ呼び込む）===========
  setwd("C:/RW")
  x <- read.table("random1.txt")            # 列名がない場合
  # または
  x <- read.table("random1.txt", header=T)  # 列名がある場合
  #===========================================================
